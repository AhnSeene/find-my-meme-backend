spring:
  sql:
    init:
      mode: always
  datasource:
    url: jdbc:h2:mem:testdb
    username: meme
    password: 1234
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
        default_batch_fetch_size: 1000
    open-in-view: false
    defer-datasource-initialization: true
  messages:
    basename: messages
    encoding: UTF-8

logging:
  level:
    org.hibernate.SQL: debug
file:
  upload:
    temp-dir: /temps
    image-dir: /images
  base-dir: C:/Users/ACY/testfilestore
server:
  base-url: http://localhost:8080

